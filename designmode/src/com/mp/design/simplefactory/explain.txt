
简单工厂
	
	定义：提供一个类，让他具有创建对象实例的功能，这样在外部调用时就无须关心具体实现，直接调用这个类即可。被创建的实例可以是接口，抽象类，也可以是具体的类。
	
	本质：选择实现
	
	优点： 1.封装：实现了对组件的封装  
	      2.解耦：实现了客户端和具体实现类的解耦
	
	缺点：
		  1.增加客户端的复杂度：如果通过参数来选择具体实现，客户端要知道具体参数是什么含义，不过可以通过选用可配置（文件）的方式来实现。
		  2.不方便扩展子工厂：一般工厂类的构造方法为私有，这样就不能写简单的工厂类的子类来改变创建接口的方法和行为了。不过通常不需要创建子类。